{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "verdict": {
      "type": "string",
      "enum": ["approve", "block"]
    },
    "summary": {
      "type": "string",
      "description": "日本語での判定理由の要約。pull requestコメントにそのまま揭載される。"
    },
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "severity": {
            "type": "string",
            "enum": ["low", "medium", "high"]
          },
          "category": {
            "type": ["string", "null"],
            "enum": ["supply_chain", "breaking_change", null],
            "description": "patch更新のreviewではnullでよい。minor/major更新のreviewでは指定すること。"
          },
          "description": {
            "type": "string"
          }
        },
        "required": ["severity", "category", "description"]
      }
    }
  },
  "required": ["verdict", "summary", "findings"]
}
